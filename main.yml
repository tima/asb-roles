# NOTE: this errors out currently. see comment below.
---
# this playbook would be generated by the service broker
# and used to trigger all service lifecycle actions
- name: Deploy sampleapp
  hosts: localhost
  connection: local
  gather_facts: no
  tasks:
    - debug: 
        var: asb_action
        verbosity: 1
    # fails because of https://github.com/ansible/ansible/issues/22571
    # it would elminate the tasks/asb.yml hack otherwise
    - import_role:
        name: sampleapp
        tasks_from: "{{ asb_action }}"

